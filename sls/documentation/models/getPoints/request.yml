name: GetPointsRequest
description: Get points request body
contentType: application/json
schema:
  pathParams:
    - name: "usernameId"
      description: "The usernameId parameter"
      schema:
        type: "string"
requestHeaders:
  - name: "Authorization"
    description: "Api key"
    schema:
      type: "string"
