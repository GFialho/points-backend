name: AddPointsRequest
description: Add points to an address
contentType: application/json
schema:
  $schema: http://json-schema.org/draft-04/schema#
  properties:
    points:
      type: string
    eventName:
      type: string
    amount:
      type: number
requestHeaders:
  - name: "Authorization"
    description: "Api key"
    schema:
      type: "string"
